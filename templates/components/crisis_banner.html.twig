{#
  crisis_banner: the pinned crisis box. Reuses the existing crisis strip verbatim
  so the crisis content stays exactly as it is across the site.
#}
{% include 'partials/crisis_strip.html.twig' %}
