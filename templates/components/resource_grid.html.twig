{# resource_grid: responsive card grid. Context: { cards } (list of card dicts). #}
<div class="rgrid" data-rgrid>
  {% for card in cards %}
    {% include 'components/service_card.html.twig' with { card: card } only %}
  {% endfor %}
</div>
